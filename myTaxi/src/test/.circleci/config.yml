version: 2.0
jobs:
  "MyTaxiTest":
    build:
      working_directory: ~/BackendTest/myTaxi

      steps:
        - run: mvn dependency:go-offline
        - run: mvn test

workflows:
  version: 2
  build:
    jobs:
      - "MyTaxiTest"